openai>=1.50.0
anthropic>=0.40.0
python-dotenv>=1.0.0
pyyaml>=6.0
pandas>=2.2.0
openpyxl>=3.1.0
numpy>=2.0.0
matplotlib>=3.9.0
nltk>=3.9.0
rouge-score>=0.1.2

# Google Sheets / Drive upload of the workbook (src/google_drive.py, used by
# scripts/build_xlsx.py). Not needed with --local-only.
google-api-python-client>=2.130.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Optional, not installed by default (large download, pulls in PyTorch):
#   pip install sentence-transformers
# enables the offline embedding fallback in src/evaluators.py when no
# OPENAI_API_KEY is set.
